import { UseObserverDirective } from './use-observer.directive';

describe('UseObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new UseObserverDirective();
    expect(directive).toBeTruthy();
  });
});
